#!/usr/bin/env bash
#untuk run
rm -rf logremot.txt
screen -d -m remot
screen -dmS remot -L -Logfile logremot.txt ssh remot -o StrictHostKeyChecking=no -R 80:localhost:80 localhost.run
host=$(cat /logremot.txt|grep tunneled|awk '{print $1}')
echo "Host: $host"
